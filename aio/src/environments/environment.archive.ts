// This is for archived sites, which are hosted at https://vX.angular.io/, where X is the major Angular version.
export const environment = {
  legacyUniversalAnalyticsId: 'UA-115919952-2', // Production id (since it is linked from the main site)
  googleAnalyticsId: 'G-BDHJXDP0GW', // Production id (since it is linked from the main site)

  production: true,
  mode: 'archive',
};
